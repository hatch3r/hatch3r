export const LEGACY_SESSION_START_HOOK_COMMAND =
  "node --input-type=module -e \"process.stdout.write('hatch3r hook bridge " +
  "(session-start-learnings): delegate this task to the hatch3r-learnings-loader custom subagent. " +
  "If subagent delegation is unavailable, follow the equivalent repository instructions and report " +
  "the result in plain text. The hook itself performs no repository mutation.\\\\n')\"";
