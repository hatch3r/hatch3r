{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "MultiEdit",
      "Write",
      "Grep",
      "Glob",
      "LS",
      "TodoRead",
      "TodoWrite"
    ],
    "deny": []
  },
  "teammateMode": "tool-using",
  "hooks": {
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "head -90 governance/CONSTITUTION.md 2>/dev/null && echo '---' && echo 'Pending audit findings:' && python3 -c \"import json; r=json.load(open('governance/audit/finding-registry.json')); p=[f for f in r.get('findings',[]) if f.get('status')=='pending']; print(f'  {len(p)} pending')\" 2>/dev/null || echo '  Registry not found'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hatch3r-dev: Changes to governance/ must serve a Binding Pillar (P1-P6). Changes to src/adapters/ require D09 checklist.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hatch3r-dev: Content artifacts require frontmatter + Pillar alignment + duplication check. Governance files must respect lean thresholds.'"
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'hatch3r-dev: Sub-agent inherits governance. 6 Binding Pillars (P1-P6) in governance/CONSTITUTION.md. All changes must serve at least one.'"
          }
        ]
      }
    ]
  }
}
